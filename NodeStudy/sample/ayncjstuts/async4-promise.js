/**
 * A promise an object that represents an action that is not finised yet, but will esentially finish down the line 
 * 
 * Helps better organize the callbacks
 */

 window.onload = function(){

    function get(){
        return new Promise((resolve, reject) => {
            
        });
    }

 }
